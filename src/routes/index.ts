/**
 * This is a utility file that re-exports the contents of the `routes` folder.
 *
 * Each component of the `routes` folder provide both the default and named export to allow maximum flexibility at the
 * cost of code standardization
 */

export { Home } from './home/index'
export { Provincia } from './provincia/index'
